# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Persisted spool correction factor -- bench/physical-spool-specific runtime
# state (exercise_tab_build_spec_layerA.md §6), not source. board_constants.
# SPOOL_CORRECTION_K_DEFAULT is the fallback when this file is absent.
config/spool_calibration.json

# Persisted experimental multi-point spool-growth calibration (train tab
# calibration overhaul) -- same bench/physical-spool-specific character as
# the sidecar above, kept separate since it's a list of points, not floats.
config/spool_growth_calibration.json

#Back-end directories and files (in ../backend/)
# Ignore venv for python
.venv/
__pycache__/
*.pyc

# Build output
dist/
build/
backend/build/
backend/dist/

# PyInstaller
*.manifest

# Note: lockfiles (package-lock.json) are committed for reproducible builds.
